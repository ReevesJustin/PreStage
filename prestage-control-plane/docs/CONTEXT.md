# Context

This project enforces filesystem-only operations by default. Source control and canonical documentation are the sole outputs. No execution work, external dependencies, or MCP access allowed. All operations must pass strict readiness gates before downstream handoff.

## [project name] Description
[short description]